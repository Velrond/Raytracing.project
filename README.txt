Raytracing
========

Installation
------------

Start the program by running the command (location_of_folder)\main.exe > \(name of image).ppm in cmd.

This will create a ppm image file of the render. Afterwards use a ppm reader(photoshop,or find one online) to view the image.

